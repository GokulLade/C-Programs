#include <stdio.h>
#include <math.h>
void main()
{
        int a, b, c;
        printf("Enter the first No");
        scanf("%d", &a);
        printf("Enter the Second No");
        scanf("%d", &b);
        c = a + b;
        printf("The addition of two number is= %d", c);
}